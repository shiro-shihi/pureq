{
  "name": "@pureq/db",
  "version": "1.3.0",
  "description": "True Universal, Dependency-Free Database Engine for Pureq",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm run test:unit",
    "test:unit": "vitest run tests",
    "test:ci": "pnpm run test:unit && tsc --build tsconfig.json"
  },
  "dependencies": {
    "@pureq/validation": "workspace:*",
    "@pureq/connectivity": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shiro-shihi/pureq.git",
    "directory": "packages/db"
  }
}
